# 选课注册系统 · 后端实现设计方案

本文档对照 `README.md`（课程需求）、前端契约 `src/frontend/projectSystem/docs/api-contract.md`、前端 Mock `src/frontend/projectSystem/src/api/mockdb.ts`，以及当前后端骨架（Controller / DTO / Entity 已齐，Service 全部 `501 Not Implemented`），整理**要落地的接口、业务约束、边界条件**，以及**从基础到完善的实现顺序**。

本阶段只做设计，不改业务代码。接口路径以现有 Controller 为准，不再另开一套 API。

---

## 0. 现状结论

后端已具备：

- REST 入口、JWT 过滤器、角色守卫、统一错误体 `{ message, errors }`
- 领域实体：账号、学生、教授、开课班次缓存、课表、课表条目、选课名额（Enrollment）、任教分配、学期配置
- 外部系统接口空壳：`CourseCatalogClient`（目录只读）、`BillingSystemClient`（计费）
- 内存 H2 + JPA `ddl-auto: update`

尚未具备：所有业务 Service 实现、种子数据、目录缓存同步、并发占座、关闭注册批处理、成绩与先修课所依赖的历史修课记录。

前端真实模式（`VITE_USE_MOCK=false`）已按下列路径调用，后端实现必须与契约对齐。

---

## 1. 系统边界

```
┌─────────────┐     只读 SQL      ┌─────────────────────┐
│ 课程目录系统 │ ◄─────────────── │ 中间层缓存           │
│ (遗留 Ingres)│   禁止回写       │ CourseOffering 快照  │
└─────────────┘                  └──────────┬──────────┘
                                            │
学生/教授/教务 ──► 本系统（选课、课表、成绩、人员、关闭注册）
                                            │
                                 关闭注册后投递     ┌─────────┐
                                            └──────►│ 计费系统 │
                                                    └─────────┘
```

**本系统负责：** 登录鉴权、师生主数据、课表保存/提交/删除、占座与退课、教授任教、关闭注册批处理、成绩录入与成绩单、向计费投递。

**本系统不负责：** 维护课程目录（代码、标题、先修、教室、时间等权威数据）；不向遗留库写入。

**运行时可变状态写在本库：** `seatsTaken`、任教教授、`cancelled` / `offeringClosed`、课表条目状态、成绩。

---

## 2. 领域状态（实现时必须守住）

### 2.1 学期与注册窗

- 当前学期：配置 `crs.term.current`（现为 `2026 秋季`），与 `term_config` 一行对应。
- `registrationClosed = false`：选课/改课表/改任教开放。
- `registrationClosed = true`：上述写操作一律 `409`；目录对学生公开真实教授名；可查名册、录成绩（针对**上一已结束学期**）。

### 2.2 课表 `StudentSchedule.status`

| 状态 | 含义 |
| --- | --- |
| `draft` | 无课表或已删除，`items` 为空 |
| `saved` | 已暂存；条目全部 `selected`，**不占座** |
| `submitted` | 已提交；主选 `enrolled` 占座，备选保持 `selected` |

约束：每名学生每个学期最多一份课表。

### 2.3 课表条目

- `type`：`primary`（主选）/ `alternate`（备选）
- `status`：`selected`（已选择未占座）/ `enrolled`（已注册占座）
- 备选 `priority`：1 最高，关闭注册调剂时按此顺序尝试
- 提交后：主选 → `enrolled`；备选永不占座，直到关闭注册时被调剂升级

### 2.4 开课班次

- 容量上限 **10**（`seatsTotal`，目录快照）
- 开班下限 **3**（关闭注册时判定，不是选课过程中的实时下限）
- 关闭注册后不足 3 人或无教授 → `cancelled = true`，并从课表剔除

---

## 3. 确定实现的接口清单

下列接口**已经挂在 Controller 上**，本方案只规定每个接口必须完成的行为。无需新增路径即可覆盖 README 八个用例；括号内为可选增强，非前端当前依赖。

### 3.1 登录（UC Login）

| 方法 | 路径 | 角色 | 成功 | 失败 |
| --- | --- | --- | --- | --- |
| POST | `/api/auth/login` | 匿名 | `200 { token, user:{id,name,role} }` | `401` 用户名或密码错误 |
| POST | `/api/auth/logout` | 已登录 | `204` | 无令牌时过滤器已 `401`；实现可为空操作 |

- 校验 `sys_user.username` + 密码哈希，签发 JWT（`JwtService` 已有）。
- 演示账号建议与 Mock 对齐：`j.davis` / `morgan` / `wang`，并保留别名 `student` / `teacher` / `admin`。
- 后置：成功则客户端持有令牌；失败系统状态不变。

### 3.2 注册窗与目录

| 方法 | 路径 | 角色 | 行为 |
| --- | --- | --- | --- |
| GET | `/api/registration/status` | 三角色 | `{ closed, term }`，读当前学期 `TermConfig` |
| GET | `/api/offerings?keyword=&dept=` | 三角色 | 当前学期开课列表；**学生**在注册关闭前 `professor` 必须空串；关闭后取消班次带 `cancelled: true` |
| GET | `/api/registrar/offerings` | 仅教务 | 同上数据但**始终真实教授名**，供总览 |

- `keyword` / `dept` 为可选过滤；前端目前客户端过滤，服务端实现过滤即可兼容。
- 目录不可用：`CourseCatalogClient.isAvailable()==false` 且本地缓存为空时，对学生返回明确错误（README 备选流），建议 `503` + message「课程目录系统不可用」。
- 不得更新遗留库；只刷新/读取本地 `course_offering` 缓存。

### 3.3 学生课表（UC Register for Courses）——核心

| 方法 | 路径 | 行为 |
| --- | --- | --- |
| GET | `/api/schedules/me` | 仅当前登录学生、当前学期课表；无记录视为 `{ status: draft, items: [], submitTime: null }` |
| PUT | `/api/schedules/me` | **暂存**：数量/重复/时间冲突校验；条目一律 `selected`；**不占座**；若原课表有 `enrolled` 须对账释放名额 |
| POST | `/api/schedules/me/submit` | **提交**：在暂存校验基础上，对尚未 enrolled 的**主选**校验先修、仍开放、未满员；通过后占座并标 `enrolled`；备选仍 `selected` |
| DELETE | `/api/schedules/me` | 确认删除语义由前端完成；服务端删除课表，已 enrolled 的班次必须从花名册/Enrollment 移除并 `seatsTaken--` |

注册已关闭：PUT/POST/DELETE → `409`「本学期注册已关闭」。

校验失败：`422`，`errors[]` 中 `code` ∈ `full | prerequisite | conflict`（与前端 `SubmitError` 一致）。

未找到课表：DELETE/更新时空课表按 draft 处理即可，不必 404（前端删除前已有确认）。

### 3.4 教授任教（UC Select Courses to Teach）

| 方法 | 路径 | 行为 |
| --- | --- | --- |
| GET | `/api/professor/eligible` | 当前教授本学期「有资格」班次 + `teaching` 标记；无可用班次可返回空数组（前端自行提示） |
| PUT | `/api/professor/teaching` | 全量覆盖本学期任教集合：取消的移除，新增的写入；校验与已选班次之间**无时间冲突** |

- 资格建议：开课 `dept` 与教授 `dept` 相同（课程项目可落地的近似；若后续有资格表再替换）。
- 注册已关闭 → `409`；时间冲突 → `409` + `errors[{offeringId, code: conflict, message}]`。
- 目录不可用且无缓存 → `503`。
- 不得修改其他教授已占用的班次：同一班次同时只允许一名授课教授；后写冲突 `409`。

### 3.5 成绩与名册（UC Submit Grades / View Report Card）

| 方法 | 路径 | 行为 |
| --- | --- | --- |
| GET | `/api/report-card/me` | 仅当前学生；按学期分组的历史成绩；无数据返回 `{ semesters: [] }`（前端提示） |
| GET | `/api/professor/offerings` | 当前教授**上一学期**所授班次 `{id,title,term,students}` |
| GET | `/api/professor/offerings/{id}/roster` | 该班次已注册学生 + 已有成绩；非本人所授 `403`；班次不存在 `404` |
| PUT | `/api/professor/offerings/{id}/grades` | 仅教授可写；`grade` ∈ `A,B,C,D,F,I` 或 `null`（允许留空后补、允许改已有成绩） |

补充（与 Mock 对齐、README 未写死）：

- 名册建议：**当前学期注册尚未关闭时**查看当学期名册 → `409`（避免注册期泄露名单）；历史学期名册不受此限。
- 成绩单只读，不改变系统状态。
- 成绩属敏感信息：禁止学生读他人成绩单、禁止教授改非本人班次。

### 3.6 教务主数据（UC Maintain Student / Professor）

| 方法 | 路径 | 行为 |
| --- | --- | --- |
| GET/POST | `/api/registrar/students` | 列表 / 新增；POST 由系统生成唯一学号并返回 |
| PUT/DELETE | `/api/registrar/students/{id}` | 更新 / 删除；不存在 `404` |
| GET/POST | `/api/registrar/professors` | 同上，工号系统生成 |
| PUT/DELETE | `/api/registrar/professors/{id}` | 不存在 `404` |

字段与契约一致：

- 学生：`name, dob, ssn, status, graduationDate`
- 教授：`name, dob, ssn, status, dept`

编号规则建议：`S` + 三位数字、`P` + 三位数字，**删除后序号不回退**（避免撞号）。新增时同步创建 `sys_user`（否则新师生无法登录）。删除策略见第 5.6 节。

### 3.7 关闭注册（UC Close Registration）

| 方法 | 路径 | 成功体 |
| --- | --- | --- |
| POST | `/api/registrar/close-registration` | `{ cancelled: string[], billed: number, leveled?: string[] }` |

**推荐批处理顺序（以 README 为准，纠正 Mock 先取消再调剂的简化）：**

1. **前置闸门**：若本学期已关闭 → `409`「注册已经关闭，不能重复执行」。若存在未完成的提交事务（行锁等待超时）→ `409`「注册进行中，请稍后重试」（对应 README「仍在进行则不得关闭」的可实现解释；课程项目不另做独立「关闭时刻」日历）。
2. **无教授则取消**：无 `teaching_assignment` / `professorId` 的班次立即取消，并从所有课表剔除，释放名额。
3. **提交达标班次**：有教授且当前 enrolled ≥ 3 的班次，对其课表条目保持 `enrolled`（已提交者无需再 commit 一次，但要保证 Enrollment 与 `seatsTaken` 一致）。
4. **调剂（level）**：仅处理 `status=submitted` 的课表，建议按 `submitTime` 升序解决名额竞争。若该课表主选 enrolled 不足 4，按备选 `priority` 依次尝试**第一个仍可用**的备选：班次未取消、有教授、先修满足、与已生效主选无时间冲突、名额未满。成功则备选升级为 `primary` + `enrolled` 并占座。无可用备选则跳过。
5. **关闭全部班次**（`offeringClosed=true`）：此时 enrolled 仍 < 3 的班次取消，并从课表剔除、释放名额。
6. **计费**：对每位在最终课表中仍有 enrolled 条目的学生，向计费系统发送「学期 + 最终课表快照」。`billed` 建议定义为**成功发起计费的学生人数**（契约字段为 number；Mock 用「确认班次 seatsTaken 之和」，实现时在注释与返回值中固定一种定义，推荐按学生人数，更符合「向学生寄送账单」）。
7. 置 `term_config.registrationClosed=true`。整段须在**同一事务**内完成领域变更；计费投递可事务提交后异步重试（见第 5.7 节）。

---

## 4. 横切规则：错误码与鉴权

| HTTP | 场景 |
| --- | --- |
| 401 | 未登录、令牌无效、登录密码错误 |
| 403 | 角色不符（学生改他人课表、非教授录成绩、非教务改师生/关闭注册、教授改非本人班次） |
| 404 | 学生/教授/班次/名册目标不存在 |
| 409 | 注册已关闭仍写课表/任教；重复关闭；任教时间冲突；注册未关闭却查当学期名册 |
| 422 | 课表校验失败（满员/先修/冲突/超数量/重复课） |
| 501 | 仅开发期占位，正式实现后不得再出现 |
| 503 | 课程目录不可用且无缓存 |

- 学生接口一律从 JWT 取 `studentId`，忽略客户端伪造的他人 id。
- 教授接口一律从 JWT 取 `professorId`。
- 课表冲突判定：**同一天且时间区间半开重叠** `a.start < b.end && b.start < a.end`；主选与主选、主选与备选、备选与备选**全部两两校验**（与前端 Mock / Catalog 页一致）。

---

## 5. 业务约束与边界条件（按模块）

### 5.1 认证

- 用户名、密码均非空（Bean Validation 已有）。
- 密码错误与用户不存在对外同一句「用户名或密码错误」，避免枚举账号。
- 令牌过期后写接口 `401`；logout 不做强制服务端黑名单亦可（契约允许空实现）。

### 5.2 目录与缓存

- 权威字段（code/title/dept/days/start/end/room/prerequisites/seatsTotal）只来自目录快照。
- 启动或定时从 `CourseCatalogClient` 拉取当前学期列表，upsert 到 `course_offering`；**禁止**把 `seatsTaken` / `cancelled` / 任教结果写回遗留库。
- 访问遗留库目标延迟 ≤ 10 秒：实现上「本地缓存命中即返回」，目录故障时仍可使用上次快照；快照为空才 503。
- 学生视图关闭注册前隐藏教授名（防止选课期按教师「刷课」——与现有前端约定一致）。

### 5.3 选课数量与占座

- 主选 ≤ 4，备选 ≤ 2；允许少于上限（不足 4 门主选在关闭注册时才调剂）。
- 同一 `offeringId` 不得在一份课表出现两次。
- **暂存不占座**；**只有提交后的主选占座**。
- 满员：`seatsTaken >= seatsTotal`（上限 10）。提交前必须再次读库，不能只信请求体里的目录数字。
- 改选：新主选集合相对旧 `enrolled` 集合做差——去掉的释放座位，新增的占座；重复提交同一门不得 `seatsTaken++`。
- 并发：`enrollment (offering_id, student_id)` 唯一约束 + 更新座位时条件写 `WHERE seats_taken < seats_total`，失败转为 `422 full`。
- 已满须在学生**提交课表前**失败返回，对应 README「满员通知」。

### 5.4 先修课

- 提交主选、以及关闭注册调剂备选时校验。
- 「已满足」定义：该生历史学期 Enrollment 中，课程 code 出现且成绩 ∈ `{A,B,C,D}`（`F`/`I`/空不计入）。若无学分及格概念，课程项目可用「历史学期存在非 F/I 成绩」近似。
- 暂存**不强制**先修（与 Mock 一致），避免学生无法保存意向课表。

### 5.5 时间冲突

- 选课提交/暂存：课表内全量两两冲突 → `422 conflict`。
- 教授任教：所选班次两两冲突 → `409`（契约对任教使用 409 而非 422，保持不变）。
- 关闭注册调剂：备选与**已生效主选**冲突则跳过该备选，不整表失败。

### 5.6 教务主数据

- 仅 Registrar 可改任何学生/教授信息。
- 删除学生：若当学期仍有 `enrolled`，须先释放名额再删课表/账号，或拒绝删除并 `409`（推荐 **有在册选课时拒绝删除**，边界更清晰）。
- 删除教授：若仍有当学期任教分配，推荐 `409`，避免关闭注册时出现「刚删掉教师导致班次被误取消」的竞态。
- SSN 等敏感字段仅教务接口返回，不出现在学生目录中。

### 5.7 关闭注册与计费

- 幂等：已关闭再次调用 `409`，不重复计费、不重复取消。
- 调剂「第一个仍可用」= 按 priority 升序第一个通过教授/人数/先修/冲突/余量检查的备选。
- 班次取消后：所有课表去掉该 `offeringId`；若因此课表被掏空，状态可保持 `submitted` 但 items 为空，或回 `saved`——建议保持 `submitted` 以留下 `submitTime` 审计。
- 计费系统不可用：领域关闭仍应成功（避免教务卡死）；将失败事务写入待重试队列，按间隔重发直至 `isAvailable()`，对应 README 备选流。Stub 阶段可写日志模拟。
- README 原文「若注册仍在进行则终止」按第 3.7 节闸门实现，**不要**理解成「注册窗未关就不能关」——否则关闭用例永远无法执行。

### 5.8 成绩

- 只有教授可写成绩；可部分提交、可修改。
- 非法成绩字符 → `400`/`422`。
- 成绩单默认「上一已结束学期」；前端展示多学期时，后端可一次返回该生全部历史学期（与现契约 `semesters[]` 一致）。
- `credits`：当前 `CourseOffering` 实体无学分字段，成绩单需要。实现选项：缓存表补 `credits`，或成绩单固定按课程 code 映射；**补字段更干净**。

### 5.9 权限隔离（补充规格 3.7）

| 主体 | 允许 | 禁止 |
| --- | --- | --- |
| 学生 | 自己的课表 CRUD、自己的成绩单、看目录 | 改他人课表、看他人成绩、维护师生、关闭注册、录成绩 |
| 教授 | 自己的任教集合、自己班次名册与成绩 | 改其他教授班次/成绩、改学生主数据 |
| 教务 | 师生 CRUD、关注册、目录总览 | 代替学生提交课表、代替教授录成绩（需求未授予） |

### 5.10 明确不在范围内

- 不提供课程目录的增删改 API。
- 不实现真实 Ingres/VAX 连接（`crs.catalog.enabled=false` 时用 Stub + 本地种子模拟只读目录）。
- 不实现真实财务扣款，只保留投递接口与重试。
- 不实现 Windows 95 桌面客户端（现有 Web UI 已覆盖交互）。
- 2000 并发等性能指标本课程以正确性与事务安全为先，不做压测达标设计。

---

## 6. README / 契约 / Mock 差异及裁定

| 点 | README | 前端 Mock/契约 | 后端采用 |
| --- | --- | --- | --- |
| 关闭时「注册进行中」 | 进行中则整用例终止 | 已关闭才 409 | 已关闭 409 + 提交事务冲突 409 |
| 取消与调剂顺序 | 先对达标班次 commit，再调剂，再按 3 人下限取消 | 先按无教授或 <3 取消，再调剂，调剂后不再二次取消 | **按 README**：调剂后再判 3 人下限 |
| `billed` 含义 | 向计费系统发每位学生事务 | seatsTaken 之和 | **按学生人数**计费事务数 |
| 名册何时可看 | 未写必须等关闭 | 未关闭 409 | 与 Mock 一致 |
| 教授名对学生可见性 | 未要求隐藏 | 关闭前空串 | 与契约一致 |
| 登录字段 | 「姓名和密码」 | username + password | 与契约一致（用户名） |
| 资格班次 | 「有资格讲授」 | Mock 写死 id 列表 | 按院系匹配，种子数据保证可演示 |

---

## 7. 数据与种子（实现前置）

建议启动时写入（或 `data.sql`），保证三条角色链路可演示：

- 学期：`2026 秋季` 未关闭；历史 `2025 秋季` / `2025 春季` 已关闭且有成绩。
- 用户：学生 `S001` Jordan Davis、教授 `P001` Alex Morgan、教务 `R001` 王丽。
- 目录快照：与 Mock 七门课对齐（含满员 `MATH-214`、先修 `CS-401`/`PHYS-150`、时间冲突对 `CS-301` vs `CS-401`）。
- 历史 Enrollment：`S001` 已修 `CS-101, MATH-110, ENG-105` 等，供先修与成绩单。
- 教授历史任教：`CS-210`、`MATH-110`，供录成绩演示。
- `seatsTotal` 一律 10；部分 `seatsTaken` 预置以演示满员。

缺失但成绩单需要的字段：开课/课程 **credits**；建议在 `course_offering` 增加，目录 Stub 一并给出。

---

## 8. 从基础到完善的开发步骤

原则：**先让系统能登录并读到数据，再做会改状态的主路径，再做批处理与成绩，最后补并发与外部系统韧性。** 每步结束后前端关 Mock 应能点通该步对应页面。

### 第 0 步 — 基础设施与种子（1 个迭代）√

**为什么最先：** 后续所有模块都依赖账号、学期、班次缓存。

- 实现登录（哈希校验 + JWT），logout 空实现即可。
- 种子：三角色用户、师生档案、当前学期 `TermConfig`、目录 Stub 写入 `course_offering`。
- 实现 `GET /registration/status`。
- 验收：`student/teacher/admin` 能登录；错误密码 401；无令牌访问业务接口 401。

### 第 1 步 — 教务主数据（基础主数据）√

**为什么靠前：** 选课、任教、关闭注册都要有真实学生/教授 id；也是三个「维护」用例中最简单的 CRUD。

- 实现学生/教授列表、新增（系统发号）、更新、删除及 404。
- 新增时创建登录账号（初始密码策略写进注释即可，如与学号相同）。
- 验收：AdminStudents / AdminProfessors 页可走通；非教务 403。

### 第 2 步 — 课程目录查询√

**为什么：** 学生选课页、教务总览、教授资格列表的数据源。

- `CourseCatalogClientStub` 返回固定当前学期班次；同步到本地缓存。
- `GET /offerings`：过滤、学生脱敏教授名、`cancelled`。
- `GET /registrar/offerings`：真实教授名。
- 验收：Catalog / AdminCourses 能列出课程；关闭前学生看不到教师名。

### 第 3 步 — 学生课表（核心用例，最高优先级业务）√

**为什么：** README 主场景；规则最多（数量、重复、冲突、先修、满员、保存 vs 提交）。

建议子增量：

1. `GET /schedules/me` 空课表 draft  
2. `PUT` 暂存：数量/重复/冲突，不占座；关闭则 409  
3. `POST` 提交：先修 + 满员 + 占座 + 名额对账；`422 errors`  
4. `DELETE` 释放 enrolled 名额  

验收：选课页加/退、暂存、提交满员课、提交缺先修、时间冲突、重复选、关闭后无法改。必须带数据库级占座，不能只改内存。

### 第 4 步 — 教授选择授课 √

**为什么：** 关闭注册「无教授则取消」依赖任教数据；独立用例且规则少于选课。

- `GET /professor/eligible` + `teaching` 标记  
- `PUT /professor/teaching` 全量替换、冲突 409、关闭 409、不得覆盖他人班次  

验收：TeachCourses 勾选/取消；两门同时间无法保存。

### 第 5 步 — 关闭注册批处理（复杂但关键）√

**为什么：** 把「意向课表」变成「最终课表 + 账单」；依赖第 3、4 步数据。

- 按第 3.7 节顺序实现单事务批处理。
- `BillingSystemClientStub` 先打日志；返回 `cancelled` / `leveled` / `billed`。
- 验收：无教师班次被取消；人数 1–2 的班次在调剂后仍不足则取消；主选不足时按备选优先级补课；已关闭不能再关；学生课表锁定。

### 第 6 步 — 成绩与成绩单 √

**为什么：** 依赖「班次已有学生」；演示可用历史学期种子，不必等第 5 步也能先做历史学期录入。

- 成绩单只读多学期。
- 教授待录列表、名册、部分录入/改成绩。
- 当学期未关闭查名册 409（可选但建议）。

验收：ReportCard 只显示本人；SubmitGrades 不能改别人的班；非法成绩拒绝。

### 第 7 步 — 完善与韧性（收口）√

按重要性排序：

1. **并发占座**：两学生同时提交最后 1 个名额，只有一人成功。  
2. **目录降级**：Stub `isAvailable=false` 时用缓存；缓存空则 503。  
3. **计费重试**：关闭成功但计费失败时队列重试，不回滚已关闭状态。  
4. **删除保护**：在册学生/任教教授删除策略落地。  
5. **先修数据完整**：历史成绩与 `COMPLETED` 集合一致，避免演示课永远 422。  
6. **学分字段** 补齐成绩单。  
7. 针对 Service 的单元/集成测试：提交校验、调剂顺序、关闭幂等、权限矩阵。

---

## 9. 建议的模块优先级总表

| 优先级 | 模块 | 对应用例 | 前端页面 | 依赖 |
| --- | --- | --- | --- | --- |
| P0 | 登录 + 学期状态 + 种子 | Login | Login | 无 |
| P1 | 师生维护 | Maintain Student/Professor | AdminStudents / AdminProfessors | P0 |
| P2 | 目录只读 | （跨用例） | Catalog / AdminCourses | P0 |
| P3 | 课表保存/提交/删除 | Register for Courses | Catalog / Schedule | P0, P2 |
| P4 | 教授任教 | Select Courses to Teach | TeachCourses | P0, P2 |
| P5 | 关闭注册 + 计费 Stub | Close Registration | AdminCloseRegistration | P3, P4 |
| P6 | 名册与成绩 / 成绩单 | Submit Grades / View Report Card | SubmitGrades / ReportCard | P0，历史数据或 P5 |
| P7 | 并发、缓存、重试、测试 | 补充规格 | — | P3–P6 |

P3 是业务核心，P5 是规则最重的批处理；P1/P2 是为了让 P3 能在真实数据上跑。不要先做成绩再做选课。

---

## 10. 每步可执行的验收清单（摘要）

- 三角色登录与路由权限与后端 403 一致。  
- 4+2 选课上限、时间冲突、先修、满员 10 人、暂存不占座、提交占座、删除释放。  
- 教授任教无冲突；关闭后学生/教授写操作全部 409。  
- 关闭注册：无教授取消 → 调剂 → 不足 3 人再取消 → 计费次数与最终课表人数相符。  
- 仅教授写成绩，仅教务改师生，学生只能看自己的成绩单。  
- 遗留目录无任何更新语句。  

按上述顺序实现，即可在不改前端契约的前提下，把当前「空框架」补成符合 README 的后端。
